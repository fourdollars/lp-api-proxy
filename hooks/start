#!/usr/bin/env bash
set -euo pipefail

# shellcheck source=hooks/common.sh
. "$(dirname "$0")/common.sh"

write_env_file
write_service_file
reload_and_restart_service
status-set active "lp-api-proxy ready at $(effective_base_url)"
