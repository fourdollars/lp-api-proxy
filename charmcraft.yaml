type: charm
base: ubuntu@24.04

platforms:
  amd64:

parts:
  charm:
    plugin: dump
    source: .
    prime:
      - hooks
      - static
      - metadata.yaml
      - config.yaml
      - main.py
      - requirements.txt
